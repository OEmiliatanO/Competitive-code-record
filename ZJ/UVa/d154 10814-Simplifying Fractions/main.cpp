#include <cstdio>
#include <algorithm>
using namespace std;
unsigned int p,q;
unsigned int gcd;
short N;
int main()
{
    scanf("%hd",&N);
    while(N--&&scanf("%u / %u",&p,&q))
    {

    }
    return 0;
}
