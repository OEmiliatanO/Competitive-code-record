#include <cstdio>

using namespace std;

int n, a, b, t, v, d;

int main()
{
    scanf("%d", &n);
    while(n-- && scanf("%d %d %d %d %d", &a, &b, &t, &v, &d))
        printf("%d\n", a * t << 1);

    return 0;
}
