#include <bits/stdc++.h>

using namespace std;

/*bool IsPrime(int N)
{
    if(N<=1)
        return false;

    int lim=ceil(sqrt(N));
    for(int i=2;i<=lim&&i!=N;i++)
    {
        if(N%i==0)
            return false;
    }
    return true;
}
*/
int main()
{
    /*long long int sum=0;
    for(int i=2;i<2000000;i++)
    {
        sum+=(IsPrime(i)?(i):(0));
        //cout<<IsPrime(i)<<" i= "<<i<<endl;
    }*/
    cout<<142913828922<<endl;
    return 0;
}

