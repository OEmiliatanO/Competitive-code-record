#include <bits/stdc++.h>

using namespace std;

int main()
{
    double D,L;
    while(cin>>D>>L)
    {
        cout<<fixed<<setprecision(3)<<acos(0)*(L/2.0)*sqrt((L+D)*(L-D))<<endl;
    }
    return 0;
}
